# CLAUDE.md rule check snapshot

**Date:** 2026-04-26
**Generated by:** `pnpm check-rules --md`

## Summary

| Check | Group                      | Total   | Per-rule                   |
| ----- | -------------------------- | ------- | -------------------------- |
| 1     | Permission & self-approval | 271     | #5: 254, #6: 17            |
| 2     | State machine & audit log  | 140     | #8: 105, #18: 35           |
| 3     | Financial / concurrency    | 215     | #19: 87, #20: 21, #21: 107 |
| 4     | Structural consistency     | 42      | #4: 22, #28: 20            |
|       | **Grand total**            | **668** |                            |

## Full output

### Check 1: Permission & self-approval

```
━━ Permission & self-approval gate check (rules #5, #6, #7) ━━

  Scanned 163 files, 924 functions.

  ❌ Rule #5 — service writes need requirePermission — 254 violation(s)
    apps/web/src/lib/accounting/interprojectLoanService.ts:242  createInterprojectLoan()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/interprojectLoanService.ts:508  recordRepayment()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:168  createPaymentBatch()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:261  updatePaymentBatch()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:294  addBatchReceipt()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:348  removeBatchReceipt()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:385  updateBatchReceipt()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:441  addBatchPayment()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:516  removeBatchPayment()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:553  updateBatchPayment()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:622  submitBatchForApproval()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:655  approveBatch()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:679  rejectBatch()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentBatchService.ts:698  cancelBatch()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/fiscalYearService.ts:205  closePeriod()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/fiscalYearService.ts:273  lockPeriod()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/fiscalYearService.ts:318  reopenPeriod()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/transactionVoidService.ts:165  voidTransaction()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/transactionVoidService.ts:272  voidAndRecreateTransaction()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/vendorBillIntegrationService.ts:21  createVendorBillFromMatch()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/bankReconciliation/crud.ts:25  createBankStatement()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/bankReconciliation/crud.ts:52  addBankTransactions()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/recurringTransactionService.ts:179  createRecurringTransaction()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/recurringTransactionService.ts:259  updateRecurringTransaction()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/recurringTransactionService.ts:299  updateRecurringTransactionStatus()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/recurringTransactionService.ts:318  deleteRecurringTransaction()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/recurringTransactionService.ts:514  skipOccurrence()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/recurringTransactionService.ts:537  modifyOccurrence()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/recurringTransactionService.ts:566  markOccurrenceGenerated()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/yearEndClosingService.ts:439  executeYearEndClosing()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/yearEndClosingService.ts:852  createClosingJournalEntry()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/yearEndClosingService.ts:927  voidClosingJournalEntry()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentPlanningService.ts:50  createManualCashFlowItem()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentPlanningService.ts:76  updateManualCashFlowItem()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/paymentPlanningService.ts:108  deleteManualCashFlowItem()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/transactionService.ts:208  saveTransaction()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/transactionService.ts:305  saveTransactionBatch()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/accounting/transactionService.ts:350  createTransactionWithUpdates()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/packingListService.ts:65  createPackingList()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/packingListService.ts:245  updatePackingListStatus()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/packingListService.ts:418  updatePackingList()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/packingListService.ts:508  removePLAttachment()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/goodsReceiptService.ts:771  updateGoodsReceiptMetadata()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/serviceOrder/crud.ts:67  createServiceOrder()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/serviceOrder/crud.ts:157  updateServiceOrderStatus()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/serviceOrder/crud.ts:195  updateServiceOrder()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/purchaseOrder/workflow.ts:366  updatePOStatus()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/rfq/crud.ts:42  createRFQ()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/rfq/crud.ts:312  createRFQFromPRs()
      service function writes to Firestore but does not call requirePermission (rule #5)
    apps/web/src/lib/procurement/rfq/crud.ts:491  updateRFQ()
      service function writes to Firestore but does not call requirePermission (rule #5)
    … and 204 more
  ❌ Rule #6 — approve/reject needs preventSelfApproval — 17 violation(s)
    apps/web/src/lib/accounting/paymentBatchService.ts:622  submitBatchForApproval()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/accounting/paymentBatchService.ts:655  approveBatch()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/accounting/paymentBatchService.ts:679  rejectBatch()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/accounting/transactionApprovalService.ts:134  submitTransactionForApproval()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/accounting/transactionApprovalService.ts:412  rejectTransaction()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/procurement/goodsReceiptService.ts:577  approveGRForPayment()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/procurement/purchaseOrder/workflow.ts:30  submitPOForApproval()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/procurement/amendment/crud.ts:153  submitAmendmentForApproval()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/procurement/amendment/crud.ts:358  rejectAmendment()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/procurement/threeWayMatch/workflow.ts:35  approveMatch()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/procurement/threeWayMatch/workflow.ts:136  rejectMatch()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/proposals/approvalWorkflow.ts:29  submitProposalForApproval()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/proposals/approvalWorkflow.ts:200  rejectProposal()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/documents/commentResolutionService.ts:92  approveCommentResolution()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/documents/commentService.ts:208  approveCommentResolution()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/documents/commentService.ts:241  rejectCommentResolution()
      approval/rejection function does not call preventSelfApproval (rule #6)
    apps/web/src/lib/vendorQuotes/vendorQuoteWorkflow.ts:190  rejectVendorQuote()
      approval/rejection function does not call preventSelfApproval (rule #6)
  ✅ Rule #7 — no hardcoded permission flags — clean

❌ Permission gate check found 271 violation(s).
```

### Check 2: State machine & audit log

```
━━ State machine & audit log enforcement (rules #8, #17, #18) ━━

  Scanned 409 files, 2043 functions.

  ❌ Rule #8 — status changes need requireValidTransition — 105 violation(s)
    apps/web/src/lib/accounting/glEntryRegeneration.ts:27  regenerateCustomerPaymentGL()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/accounting/glEntryRegeneration.ts:100  regenerateVendorPaymentGL()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/accounting/interprojectLoanService.ts:242  createInterprojectLoan()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/interprojectLoanService.ts:508  recordRepayment()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/paymentBatchService.ts:294  addBatchReceipt()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/accounting/paymentBatchService.ts:348  removeBatchReceipt()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/accounting/paymentBatchService.ts:385  updateBatchReceipt()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/accounting/paymentBatchService.ts:441  addBatchPayment()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/paymentBatchService.ts:516  removeBatchPayment()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/accounting/paymentBatchService.ts:553  updateBatchPayment()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/accounting/fiscalYearService.ts:205  closePeriod()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/fiscalYearService.ts:273  lockPeriod()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/fiscalYearService.ts:318  reopenPeriod()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/transactionVoidService.ts:165  voidTransaction()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/transactionApprovalService.ts:134  submitTransactionForApproval()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/transactionApprovalService.ts:269  approveTransaction()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/transactionApprovalService.ts:412  rejectTransaction()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/fixedAssetService.ts:321  disposeAsset()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/fixedAssetService.ts:358  writeOffAsset()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/bankReconciliation/reporting.ts:155  markStatementAsReconciled()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/recurringTransactionService.ts:514  skipOccurrence()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/recurringTransactionService.ts:537  modifyOccurrence()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/recurringTransactionService.ts:566  markOccurrenceGenerated()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/yearEndClosingService.ts:439  executeYearEndClosing()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/yearEndClosingService.ts:852  createClosingJournalEntry()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/accounting/yearEndClosingService.ts:927  voidClosingJournalEntry()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/packingListService.ts:65  createPackingList()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/packingListService.ts:245  updatePackingListStatus()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/procurement/packingListService.ts:418  updatePackingList()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/procurement/goodsReceiptService.ts:71  createGoodsReceipt()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/goodsReceiptService.ts:412  completeGR()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/goodsReceiptService.ts:577  approveGRForPayment()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/procurement/goodsReceiptService.ts:771  updateGoodsReceiptMetadata()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/procurement/purchaseOrder/crud.ts:623  updateDraftPO()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/procurement/purchaseOrder/workflow.ts:30  submitPOForApproval()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/purchaseOrder/workflow.ts:108  approvePO()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/purchaseOrder/workflow.ts:232  rejectPO()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/purchaseOrder/workflow.ts:302  issuePO()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/procurementDeleteService.ts:344  softDeletePackingList()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/procurement/rfq/crud.ts:42  createRFQ()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/rfq/crud.ts:312  createRFQFromPRs()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/amendment/crud.ts:153  submitAmendmentForApproval()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/amendment/crud.ts:225  approveAmendment()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/amendment/crud.ts:358  rejectAmendment()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/purchaseRequest/workflow.ts:28  submitPurchaseRequestForApproval()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/purchaseRequest/workflow.ts:157  approvePurchaseRequest()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/purchaseRequest/workflow.ts:315  rejectPurchaseRequest()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/purchaseRequest/workflow.ts:453  addPurchaseRequestComment()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    apps/web/src/lib/procurement/threeWayMatch/matching.ts:52  performThreeWayMatch()
      function gates a write on a status comparison (ad-hoc state machine — rule #8)
    apps/web/src/lib/procurement/accountingIntegration.ts:79  createBillFromGoodsReceipt()
      function writes a status literal to Firestore without calling requireValidTransition (rule #8)
    … and 55 more
  ✅ Rule #17 — state machines live in stateMachines.ts — clean
  ❌ Rule #18 — sensitive ops need an audit log call — 35 violation(s)
    apps/web/src/lib/accounting/paymentBatchService.ts:622  submitBatchForApproval()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/accounting/paymentBatchService.ts:655  approveBatch()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/accounting/paymentBatchService.ts:679  rejectBatch()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/accounting/fiscalYearService.ts:205  closePeriod()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/accounting/fiscalYearService.ts:273  lockPeriod()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/accounting/fiscalYearService.ts:318  reopenPeriod()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/accounting/yearEndClosingService.ts:927  voidClosingJournalEntry()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/procurement/goodsReceiptService.ts:577  approveGRForPayment()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/procurement/amendment/crud.ts:153  submitAmendmentForApproval()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/procurement/amendment/crud.ts:225  approveAmendment()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/procurement/amendment/crud.ts:358  rejectAmendment()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/proposals/approvalWorkflow.ts:29  submitProposalForApproval()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/proposals/approvalWorkflow.ts:104  approveProposal()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/proposals/approvalWorkflow.ts:200  rejectProposal()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/proposals/proposalTemplateService.ts:186  deleteProposalTemplate()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/enquiry/enquiryService.ts:744  deleteEnquiryDocument()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/hr/holidays/holidayService.ts:196  deleteHoliday()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/hr/travelExpenses/travelExpenseApprovalService.ts:232  approveTravelExpenseReport()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/hr/travelExpenses/travelExpenseApprovalService.ts:329  rejectTravelExpenseReport()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/hr/onDuty/onDutyApprovalService.ts:343  approveOnDutyRequest()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/hr/onDuty/onDutyApprovalService.ts:543  rejectOnDutyRequest()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/hr/leaves/leaveApprovalService.ts:366  approveLeaveRequest()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/hr/leaves/leaveApprovalService.ts:566  rejectLeaveRequest()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/hr/leaves/leaveRequestService.ts:575  deleteLeaveRequest()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/feedback/feedbackTaskService.ts:150  closeFeedbackFromTask()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/documents/commentResolutionService.ts:92  approveCommentResolution()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/documents/commentService.ts:208  approveCommentResolution()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/documents/commentService.ts:241  rejectCommentResolution()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/documents/folderService.ts:496  deleteFolder()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/tasks/meetingService.ts:360  finalizeMeeting()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/thermal/savedCalculationService.ts:86  deleteCalculation()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/companyDocuments/companyDocumentService.ts:329  deleteCompanyDocument()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/projects/documentRequirementService.ts:106  deleteDocumentRequirement()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/projects/charterProcurementService.ts:122  deleteProcurementItem()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)
    apps/web/src/lib/boughtOut/boughtOutService.ts:176  deleteBoughtOutItem()
      sensitive workflow function writes data but does not log to auditLogs (rule #18)

❌ State machine & audit log check found 140 violation(s).
```

### Check 3: Financial / concurrency

```
━━ Financial math, concurrency, and soft-delete check (rules #3, #19, #20, #21) ━━

  Scanned 1613 files.

  ✅ Rule #3 — no where(isDeleted, !=, true) queries — clean
  ❌ Rule #19 — read+write needs runTransaction — 87 violation(s)
    apps/web/src/lib/accounting/glEntryRegeneration.ts:27  regenerateCustomerPaymentGL()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/glEntryRegeneration.ts:100  regenerateVendorPaymentGL()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/costCentreService.ts:22  createProjectCostCentre()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/transactionVoidService.ts:165  voidTransaction()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/transactionApprovalService.ts:134  submitTransactionForApproval()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/transactionApprovalService.ts:269  approveTransaction()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/transactionApprovalService.ts:412  rejectTransaction()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/recurringTransactionService.ts:318  deleteRecurringTransaction()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/yearEndClosingService.ts:852  createClosingJournalEntry()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/transactionDeleteService.ts:106  softDeleteTransaction()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/transactionDeleteService.ts:201  restoreTransaction()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/accounting/transactionDeleteService.ts:290  hardDeleteTransaction()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/serviceOrder/crud.ts:157  updateServiceOrderStatus()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/purchaseOrder/crud.ts:96  createPOFromOffer()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/purchaseOrder/crud.ts:623  updateDraftPO()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/procurementDeleteService.ts:36  softDeletePurchaseRequest()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/procurementDeleteService.ts:116  softDeleteRFQ()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/procurementDeleteService.ts:186  softDeletePurchaseOrder()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/procurementDeleteService.ts:265  softDeleteGoodsReceipt()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/procurementDeleteService.ts:344  softDeletePackingList()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/procurementDeleteService.ts:418  softDeleteAmendment()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/procurementDeleteService.ts:512  restoreProcurementDocument()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/purchaseRequest/crud.ts:375  updatePurchaseRequest()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/purchaseRequest/workflow.ts:28  submitPurchaseRequestForApproval()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/purchaseRequest/workflow.ts:157  approvePurchaseRequest()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/purchaseRequest/workflow.ts:315  rejectPurchaseRequest()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/purchaseRequest/workflow.ts:453  addPurchaseRequestComment()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/procurement/threeWayMatch/workflow.ts:35  approveMatch()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/proposals/approvalWorkflow.ts:29  submitProposalForApproval()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/proposals/approvalWorkflow.ts:104  approveProposal()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/proposals/approvalWorkflow.ts:200  rejectProposal()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/proposals/approvalWorkflow.ts:289  requestProposalChanges()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/proposals/approvalWorkflow.ts:379  markProposalAsSubmitted()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/proposals/approvalWorkflow.ts:421  updateProposalStatus()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/proposals/proposalService.ts:107  createProposal()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/proposals/proposalService.ts:213  createMinimalProposal()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/proposals/proposalTemplateService.ts:201  incrementTemplateUsage()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/enquiry/enquiryService.ts:433  uploadEnquiryDocument()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/enquiry/enquiryService.ts:611  reviseBidDecision()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/enquiry/enquiryService.ts:744  deleteEnquiryDocument()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/hr/employees/employeeService.ts:150  updateEmployeeHRProfile()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/hr/employees/employeeService.ts:227  updateEmployeeBasicInfo()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/hr/holidays/holidayWorkingService.ts:189  processHolidayWorkingOverride()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/hr/travelExpenses/travelExpenseService.ts:48  generateReportNumber()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/hr/onDuty/onDutyRequestService.ts:92  generateOnDutyRequestNumber()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/hr/onDuty/compOffService.ts:35  ensureCompOffBalanceExists()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/hr/leaves/leaveRequestService.ts:208  generateLeaveRequestNumber()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/feedback/feedbackTaskService.ts:102  addFollowUpToFeedback()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/feedback/feedbackTaskService.ts:150  closeFeedbackFromTask()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    apps/web/src/lib/bom/bomService.ts:50  generateBOMCode()
      function reads (getDoc) and writes (updateDoc/setDoc) without runTransaction (rule #19)
    … and 37 more
  ❌ Rule #20 — batch ops in loops need 500-op chunking — 21 violation(s)
    apps/web/src/components/projects/EditProjectDialog.tsx:66  EditProjectDialog()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/components/projects/EditProjectDialog.tsx:252  handleSubmit()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/components/projects/CreateProjectDialog.tsx:60  CreateProjectDialog()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/components/projects/CreateProjectDialog.tsx:232  handleSubmit()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/accounting/bankReconciliation/matching.ts:289  matchMultipleTransactions()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/accounting/paymentHelpers.ts:386  createPaymentWithAllocationsAtomic()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/procurement/packingListService.ts:65  createPackingList()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/procurement/purchaseOrder/crud.ts:96  createPOFromOffer()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/procurement/rfq/crud.ts:312  createRFQFromPRs()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/procurement/threeWayMatch/matching.ts:52  performThreeWayMatch()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/initializeChartOfAccounts.ts:34  initializeChartOfAccounts()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/hr/leaves/leaveBalanceService.ts:158  initializeUserLeaveBalances()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/bom/bomService.ts:469  deleteBOMItem()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/documents/masterDocumentService.ts:607  bulkCreateMasterDocuments()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/documents/linkService.ts:318  updateLinksStatus()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/documents/folderService.ts:591  moveDocumentsToFolder()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/lib/tasks/mentionService.ts:219  markMentionsAsRead()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    apps/web/src/app/procurement/purchase-requests/[id]/edit/EditPRClient.tsx:308  handleSave()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    functions/src/currency.ts:169  storeRates()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    functions/src/accountBalances.ts:77  applyBalanceChanges()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
    functions/src/procurementPaymentStatus.ts:45  syncPOPaymentToGRs()
      batch.<op> inside a loop without 500-op chunking — risks Firestore limit (rule #20)
  ❌ Rule #21 — no fallback chains on amount fields — 107 violation(s)
    apps/web/src/components/accounting/ApproveTransactionDialog.tsx:203
      amount field with || fallback — could mask zero vs missing (rule #21)
      > {formatCurrency(transaction.totalAmount || 0, transaction.currency || 'INR')}
    apps/web/src/components/accounting/SubmitForApprovalDialog.tsx:166
      amount field with || fallback — could mask zero vs missing (rule #21)
      > {formatCurrency(transaction.totalAmount || 0, transaction.currency || 'INR')}
    apps/web/src/components/accounting/TransactionAllocationTable.tsx:135
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const originalAmount = transaction.totalAmount || 0;
    apps/web/src/components/accounting/TransactionAllocationTable.tsx:191
      amount field with || fallback — could mask zero vs missing (rule #21)
      > value={allocation?.allocatedAmount || 0}
    apps/web/src/components/accounting/TransactionAllocationTable.tsx:304
      amount field with || fallback — could mask zero vs missing (rule #21)
      > value={allocation?.allocatedAmount || 0}
    apps/web/src/components/accounting/CostCentreTransactionTable.tsx:127
      amount field with || fallback — could mask zero vs missing (rule #21)
      > {formatCurrency(transaction.paidAmount || 0, transaction.currency)}
    apps/web/src/components/hr/travelExpenses/ReceiptParsingUploader.tsx:284
      amount field with || fallback — could mask zero vs missing (rule #21)
      > amount: data.totalAmount || 0,
    apps/web/src/lib/accounting/gstReports/generators.ts:74
      amount field with || fallback — could mask zero vs missing (rule #21)
      > invoiceValue: invoice.totalAmount || 0,
    apps/web/src/lib/accounting/gstReports/generators.ts:96
      amount field with || fallback — could mask zero vs missing (rule #21)
      > invoiceValue: invoice.totalAmount || 0,
    apps/web/src/lib/accounting/gstReports/generators.ts:225
      amount field with || fallback — could mask zero vs missing (rule #21)
      > billValue: bill.totalAmount || 0,
    apps/web/src/lib/accounting/transactionHelpers.ts:167
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const vendorPayable = (bill.totalAmount || 0) - tdsAmount;
    apps/web/src/lib/accounting/paymentHelpers.ts:175
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmountINR = transactionData.baseAmount || transactionData.totalAmount || 0;
    apps/web/src/lib/accounting/paymentHelpers.ts:249
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmountINR = data.baseAmount || data.totalAmount || 0;
    apps/web/src/lib/accounting/paymentHelpers.ts:294
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmount = transactionData.baseAmount || transactionData.totalAmount || 0;
    apps/web/src/lib/accounting/paymentHelpers.ts:323
      amount field with || fallback — could mask zero vs missing (rule #21)
      > totalPaid += allocation.allocatedAmount || 0;
    apps/web/src/lib/accounting/paymentHelpers.ts:476
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmountINR = invoiceData.baseAmount || invoiceData.totalAmount || 0;
    apps/web/src/lib/accounting/paymentHelpers.ts:640
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmountINR = invoiceData.baseAmount || invoiceData.totalAmount || 0;
    apps/web/src/lib/accounting/paymentHelpers.ts:727
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmountINR = data.baseAmount || data.totalAmount || 0;
    apps/web/src/lib/accounting/paymentHelpers.ts:896
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmountINR = data.baseAmount || data.totalAmount || 0;
    apps/web/src/lib/accounting/paymentHelpers.ts:988
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmountINR = data.baseAmount || data.totalAmount || 0;
    apps/web/src/lib/accounting/paymentHelpers.ts:1044
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmountINR = data.baseAmount || data.totalAmount || 0;
    apps/web/src/lib/accounting/paymentHelpers.ts:1126
      amount field with || fallback — could mask zero vs missing (rule #21)
      > (sum: number, a: PaymentAllocation) => sum + (a.allocatedAmount || 0),
    apps/web/src/lib/accounting/paymentHelpers.ts:1146
      amount field with || fallback — could mask zero vs missing (rule #21)
      > (sum: number, a: PaymentAllocation) => sum + (a.allocatedAmount || 0),
    apps/web/src/lib/accounting/paymentHelpers.ts:1220
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmountINR = data.baseAmount || data.totalAmount || 0;
    apps/web/src/lib/accounting/autoMatching/matching.ts:114
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAmount = combo.reduce((sum, t) => sum + (t.amount || t.totalAmount || 0), 0);
    apps/web/src/lib/accounting/autoMatching/scoring.ts:48
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const accAmount = accountingTxn.amount || accountingTxn.totalAmount || 0;
    apps/web/src/lib/accounting/paymentPlanningService.ts:213
      fallback chain on amount field — derive instead of cache (rule #21)
      > const amount = (data.totalAmount ?? data.amount ?? 0) - (data.paidAmount ?? 0);
    apps/web/src/lib/accounting/paymentPlanningService.ts:264
      fallback chain on amount field — derive instead of cache (rule #21)
      > const amount = (data.totalAmount ?? data.amount ?? 0) - (data.paidAmount ?? 0);
    apps/web/src/lib/accounting/reports/periodReportData.ts:367
      fallback chain on amount field — derive instead of cache (rule #21)
      > const amount = Number(data.baseAmount ?? data.totalAmount ?? 0);
    apps/web/src/lib/accounting/reports/periodReportData.ts:392
      fallback chain on amount field — derive instead of cache (rule #21)
      > const amount = Number(data.baseAmount ?? data.totalAmount ?? 0);
    apps/web/src/lib/procurement/accountingIntegration.ts:571
      amount field with || fallback — could mask zero vs missing (rule #21)
      > poTotalAmount: po?.grandTotal || 0,
    apps/web/src/lib/vendorQuotes/vendorQuoteService.ts:209
      fallback chain on amount field — derive instead of cache (rule #21)
      > const totalAmount = input.totalAmount ?? subtotal + taxAmount;
    apps/web/src/app/accounting/data-health/unmapped-accounts/page.tsx:110
      amount field with || fallback — could mask zero vs missing (rule #21)
      > totalAmount: data.totalAmount || 0,
    apps/web/src/app/accounting/data-health/unmapped-accounts/page.tsx:130
      amount field with || fallback — could mask zero vs missing (rule #21)
      > totalAmount: data.totalAmount || 0,
    apps/web/src/app/accounting/data-health/unapplied-payments/page.tsx:119
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAllocated = allocations.reduce((sum, a) => sum + (a.allocatedAmount || 0), 0);
    apps/web/src/app/accounting/data-health/unapplied-payments/page.tsx:133
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalAllocated = allocations.reduce((sum, a) => sum + (a.allocatedAmount || 0), 0);
    apps/web/src/app/accounting/data-health/overdue/page.tsx:163
      amount field with || fallback — could mask zero vs missing (rule #21)
      > addToEntityBalance(data.entityId, data.baseAmount || data.totalAmount || 0);
    apps/web/src/app/accounting/data-health/overdue/page.tsx:168
      amount field with || fallback — could mask zero vs missing (rule #21)
      > addToEntityBalance(data.entityId, -(data.baseAmount || data.totalAmount || 0));
    apps/web/src/app/accounting/data-health/overdue/page.tsx:249
      fallback chain on amount field — derive instead of cache (rule #21)
      > const outstanding = data.outstandingAmount ?? data.baseAmount ?? data.totalAmount ?? 0;
    apps/web/src/app/accounting/data-health/overdue/page.tsx:259
      amount field with || fallback — could mask zero vs missing (rule #21)
      > totalAmount: data.totalAmount || 0,
    apps/web/src/app/accounting/data-health/overdue/page.tsx:299
      fallback chain on amount field — derive instead of cache (rule #21)
      > const outstanding = data.outstandingAmount ?? data.baseAmount ?? data.totalAmount ?? 0;
    apps/web/src/app/accounting/data-health/overdue/page.tsx:309
      amount field with || fallback — could mask zero vs missing (rule #21)
      > totalAmount: data.totalAmount || 0,
    apps/web/src/app/accounting/data-health/page.tsx:174
      amount field with || fallback — could mask zero vs missing (rule #21)
      > (a: { allocatedAmount?: number }) => (a.allocatedAmount || 0) > 0
    apps/web/src/app/accounting/data-health/page.tsx:248
      amount field with || fallback — could mask zero vs missing (rule #21)
      > const totalINR = data.baseAmount || data.totalAmount || 0;
    apps/web/src/app/accounting/data-health/page.tsx:277
      amount field with || fallback — could mask zero vs missing (rule #21)
      > addToEntityBalance(data.entityId, data.baseAmount || data.totalAmount || 0);
    apps/web/src/app/accounting/data-health/page.tsx:281
      amount field with || fallback — could mask zero vs missing (rule #21)
      > addToEntityBalance(data.entityId, -(data.baseAmount || data.totalAmount || 0));
    apps/web/src/app/accounting/data-health/page.tsx:370
      fallback chain on amount field — derive instead of cache (rule #21)
      > data.outstandingAmount ?? data.baseAmount ?? data.totalAmount ?? 0;
    apps/web/src/app/accounting/data-health/stale-payments/page.tsx:156
      amount field with || fallback — could mask zero vs missing (rule #21)
      > addToEntityBalance(data.entityId, data.baseAmount || data.totalAmount || 0);
    apps/web/src/app/accounting/data-health/stale-payments/page.tsx:160
      amount field with || fallback — could mask zero vs missing (rule #21)
      > addToEntityBalance(data.entityId, -(data.baseAmount || data.totalAmount || 0));
    apps/web/src/app/accounting/payments/components/RecordVendorPaymentDialog.tsx:128
      fallback chain on amount field — derive instead of cache (rule #21)
      > const totalINR = data.baseAmount ?? data.totalAmount ?? 0;
```

### Check 4: Structural consistency

```
━━ Structural consistency check (rules #4, #24, #28) ━━

  ❌ Rule #4 — collections referenced in code need firestore.rules entries — 22 violation(s)
    amendmentApprovalHistory
      collection "amendmentApprovalHistory" is referenced in code but has no match block in firestore.rules (rule #4)
    bankStatements
      collection "bankStatements" is referenced in code but has no match block in firestore.rules (rule #4)
    bankTransactions
      collection "bankTransactions" is referenced in code but has no match block in firestore.rules (rule #4)
    companies
      collection "companies" is referenced in code but has no match block in firestore.rules (rule #4)
    entity_contacts
      collection "entity_contacts" is referenced in code but has no match block in firestore.rules (rule #4)
    fiscalYears
      collection "fiscalYears" is referenced in code but has no match block in firestore.rules (rule #4)
    interprojectLoans
      collection "interprojectLoans" is referenced in code but has no match block in firestore.rules (rule #4)
    journal_entries
      collection "journal_entries" is referenced in code but has no match block in firestore.rules (rule #4)
    ledger_entries
      collection "ledger_entries" is referenced in code but has no match block in firestore.rules (rule #4)
    matchDiscrepancies
      collection "matchDiscrepancies" is referenced in code but has no match block in firestore.rules (rule #4)
    matchLineItems
      collection "matchLineItems" is referenced in code but has no match block in firestore.rules (rule #4)
    matchToleranceConfigs
      collection "matchToleranceConfigs" is referenced in code but has no match block in firestore.rules (rule #4)
    materialPrices
      collection "materialPrices" is referenced in code but has no match block in firestore.rules (rule #4)
    projectChannels
      collection "projectChannels" is referenced in code but has no match block in firestore.rules (rule #4)
    purchaseOrderVersions
      collection "purchaseOrderVersions" is referenced in code but has no match block in firestore.rules (rule #4)
    reconciliationMatches
      collection "reconciliationMatches" is referenced in code but has no match block in firestore.rules (rule #4)
    reconciliationReports
      collection "reconciliationReports" is referenced in code but has no match block in firestore.rules (rule #4)
    serviceOrders
      collection "serviceOrders" is referenced in code but has no match block in firestore.rules (rule #4)
    shapes
      collection "shapes" is referenced in code but has no match block in firestore.rules (rule #4)
    systemConfig
      collection "systemConfig" is referenced in code but has no match block in firestore.rules (rule #4)
    threeWayMatches
      collection "threeWayMatches" is referenced in code but has no match block in firestore.rules (rule #4)
    yearEndClosingEntries
      collection "yearEndClosingEntries" is referenced in code but has no match block in firestore.rules (rule #4)
  ✅ Rule #24 — TransactionType switches must be exhaustive (no `default:`) — clean
  ❌ Rule #28 — modules need List + New + View + Edit pages — 20 violation(s)
    apps/web/src/app/accounting/cost-centres
      module incomplete — missing New, Edit (rule #28)
    apps/web/src/app/accounting/fixed-assets
      module incomplete — missing New, Edit (rule #28)
    apps/web/src/app/accounting/payment-batches
      module incomplete — missing New, Edit (rule #28)
    apps/web/src/app/accounting/recurring
      module incomplete — missing Edit (rule #28)
    apps/web/src/app/bought-out
      module incomplete — missing Edit (rule #28)
    apps/web/src/app/documents
      module incomplete — missing New, Edit (rule #28)
    apps/web/src/app/estimation
      module incomplete — missing Edit (rule #28)
    apps/web/src/app/hr/employees
      module incomplete — missing New, Edit (rule #28)
    apps/web/src/app/hr/leaves
      module incomplete — missing Edit (rule #28)
    apps/web/src/app/hr/travel-expenses
      module incomplete — missing Edit (rule #28)
    apps/web/src/app/materials/pipes
      module incomplete — missing View, Edit (rule #28)
    apps/web/src/app/materials/plates
      module incomplete — missing View, Edit (rule #28)
    apps/web/src/app/materials/vendor-offers
      module incomplete — missing Edit (rule #28)
    apps/web/src/app/procurement/amendments
      module incomplete — missing Edit (rule #28)
    apps/web/src/app/procurement/service-orders
      module incomplete — missing Edit (rule #28)
    apps/web/src/app/procurement/three-way-match
      module incomplete — missing Edit (rule #28)
    apps/web/src/app/procurement/work-completion
      module incomplete — missing Edit (rule #28)
    apps/web/src/app/projects
      module incomplete — missing New, Edit (rule #28)
    apps/web/src/app/proposals
      module incomplete — missing New, Edit (rule #28)
    apps/web/src/app/proposals/enquiries
      module incomplete — missing New (rule #28)

❌ Structural check found 42 violation(s).
```
