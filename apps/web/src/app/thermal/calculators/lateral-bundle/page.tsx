import LateralBundleClient from './LateralBundleClient';

export const metadata = {
  title: 'Lateral Tube Bundle | Thermal Calculators',
  description:
    'Design a half-shell lateral tube bundle with triangular pitch, diagonal vapour lanes, and integrated spray nozzle selection.',
};

export default function LateralBundlePage() {
  return <LateralBundleClient />;
}
