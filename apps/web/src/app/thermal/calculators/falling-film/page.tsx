import FallingFilmClient from './FallingFilmClient';

export const metadata = {
  title: 'Falling Film Evaporator | Thermal Calculators',
  description:
    'Design falling film evaporators with wetting rate analysis, heat transfer coefficients, and tube bundle layout.',
};

export default function FallingFilmPage() {
  return <FallingFilmClient />;
}
