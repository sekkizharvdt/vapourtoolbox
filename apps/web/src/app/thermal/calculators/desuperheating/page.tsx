import DesuperheatingClient from './DesuperheatingClient';

export const metadata = {
  title: 'Desuperheating Calculator | Thermal Calculators',
  description: 'Calculate spray water requirement to desuperheat steam to target temperature',
};

export default function DesuperheatingPage() {
  return <DesuperheatingClient />;
}
