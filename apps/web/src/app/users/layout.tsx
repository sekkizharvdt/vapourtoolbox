'use client';

/**
 * Legacy Users Layout
 *
 * Simplified layout for redirect pages.
 * The actual user management is now in /admin/users.
 */

export default function UsersLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
