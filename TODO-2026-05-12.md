# Vapour Toolbox — Active TODO List (2026-05-12)

Captures action items from the four-area audit on 2026-05-12: thermal AI-readiness, user-guide currency, audit-trail + versioning, and bank reconciliation deprecation.

---

## 1. Thermal calculators — AI agent integration

**Status:** Architecturally ready. All ~32 active calculators are pure functions in [apps/web/src/lib/thermal/](apps/web/src/lib/thermal/) with typed inputs/outputs, input validation, and zero browser/Firestore dependencies. The roadmap at [AI-AGENT-ROADMAP-2026-04-25.md](AI-AGENT-ROADMAP-2026-04-25.md) puts MCP exposure in Phase 6.

**Phase 1 — ship the MCP server (~3 days):**

- [ ] Create `mcp-servers/thermal-tools/` following the pattern of [mcp-servers/accounting-audit/](mcp-servers/accounting-audit/) and [mcp-servers/firebase-feedback/](mcp-servers/firebase-feedback/)
- [ ] Wrap the 10 highest-value calculators as MCP tools with Zod schemas:
  - [ ] `calculateThermalExpansion` — [thermalExpansionCalculator.ts](apps/web/src/lib/thermal/thermalExpansionCalculator.ts)
  - [ ] `lookupSteamTable` — [steamTables.ts](packages/constants/src/thermal/steamTables.ts)
  - [ ] `seawaterProperties` — [fluidProperties.ts](apps/web/src/lib/thermal/fluidProperties.ts)
  - [ ] `calculateMEDPlant` — [med/plantDesigner.ts](apps/web/src/lib/thermal/med/plantDesigner.ts)
  - [ ] `calculateHeatExchanger` — [iterativeHXDesign.ts](apps/web/src/lib/thermal/iterativeHXDesign.ts)
  - [ ] `calculatePumpSizing` — [pumpSizing.ts](apps/web/src/lib/thermal/pumpSizing.ts)
  - [ ] `calculatePipeSizing` — [pipeService.ts](apps/web/src/lib/thermal/pipeService.ts)
  - [ ] `calculatePressureDrop` — [pressureDropCalculator.ts](apps/web/src/lib/thermal/pressureDropCalculator.ts)
  - [ ] `calculateTVC` — [tvcCalculator.ts](apps/web/src/lib/thermal/tvcCalculator.ts)
  - [ ] `calculateDemister` — [demisterCalculator.ts](apps/web/src/lib/thermal/demisterCalculator.ts)
- [ ] Add explicit `unit:` tags to every numeric input in the tool schemas (mm, °C, bar, kg/s) so agents don't pass meters where mm is expected
- [ ] Hook tool invocations into the existing audit-log pipeline ([apps/web/src/lib/audit/](apps/web/src/lib/audit/)) so every agent call is recorded

**Phase 2 — polish (~1 week, optional):**

- [ ] Add simplified entry points (`quickMEDDesign`, `quickHeatExchangerSize`) that wrap the 30+ field calculators with smart defaults
- [ ] Add `verbosity: 'summary' | 'detailed'` flag so agents can return intermediate steps for human-facing explanations
- [ ] Extract calculator library to its own package `@vapour/thermal-calculators` for use from cloud functions / orchestrator

**Decision point:** ship Phase 1 now, or wait until a procurement / estimation use case demands it (current roadmap stance).

---

## 2. User guides — DONE (2026-05-12)

**Status:** Every actionable user-guide item from the audit is done. Standalone guides for Projects / Proposals / Procurement / Flow / Estimation / Materials / Documents already exist as in-app `UserGuide` sections — that is the canonical surface and we are not adding parallel markdown docs.

- [x] Updated [ChangelogSection.tsx](apps/web/src/components/common/UserGuide/ChangelogSection.tsx) with the v1.6.0 entry covering ~95 commits since v1.5.0
- [x] Added a detailed Thermal Expansion section to [ThermalCalculatorsSection.tsx](apps/web/src/components/common/UserGuide/ThermalCalculatorsSection.tsx) covering supported materials (Carbon Steel, SS 304/304L, SS 316/316L, Duplex 2205, Copper, Cupronickel 90/10 and 70/30), Free vs Restrained constraint modes, and yield-utilisation thresholds
- [x] Added a Full Calculator Catalog at the bottom of [ThermalCalculatorsSection.tsx](apps/web/src/components/common/UserGuide/ThermalCalculatorsSection.tsx) — five tables grouping the remaining ~30 calculators by domain: fluid properties, heat transfer, MED-specific, vapour/vacuum, hydraulics, mechanical/chemistry
- [x] Created [FeedbackSection.tsx](apps/web/src/components/common/UserGuide/FeedbackSection.tsx) and wired it into the in-app guide — covers submission, the five-state workflow, close-from-any-status, and writing-good-feedback tips
- [x] Refreshed accounting guide as [ACCOUNTING-FEATURE-GUIDE-2026-05-12.md](ACCOUNTING-FEATURE-GUIDE-2026-05-12.md); old 2026-04-24 version removed; reflects bank-reconciliation deletion, audit trail closure (rule 18), and agent infrastructure groundwork
- [x] Refreshed [docs/hr/USER-GUIDE.md](docs/hr/USER-GUIDE.md) with v1.6.0 changes section + bumped Last Updated to 2026-05-12 (comp-off limits and holiday overrides were already documented correctly in §2.5 and §3.3 — the original audit claim of "missing" was incorrect)
- [x] **Decision** — all per-module documentation lives in the in-app UserGuide accordion sections. Markdown docs at repo root (`*-GUIDE-YYYY-MM-DD.md`) stay reserved for point-in-time architectural snapshots / audits, not user-facing how-tos

---

## 3. Audit trail + versioning

### Audit trail — DONE (2026-05-12)

**Status:** 50+ `logAuditEvent()` call sites cover every sensitive operation. Rule wording is now correct and the admin read surface has a stats strip.

- [x] Updated CLAUDE.md rule 18 — removed "(pattern in progress)", added concrete details on `logAuditEvent()` usage, the admin Activity Feed surface, and agent-action fields (`actorType`, `agentRunId`, `agentToolName`)
- [x] Added a summary stats strip to the admin activity feed at [admin/activity/page.tsx](apps/web/src/app/admin/activity/page.tsx) — six cards showing total entries, critical count, failed count, agent-action count, top module, and top actor over the most-recent-100 window

### Versioning — DONE (2026-05-12)

**Status:** Single source of truth established. App, packages, and root CHANGELOG.md all agree on `1.6.0`.

- [x] Single source of truth → [packages/constants/src/changelog.ts](packages/constants/src/changelog.ts) (typed `CHANGELOG` array, consumed by both the in-app guide and the generated [CHANGELOG.md](CHANGELOG.md))
- [x] Bumped `APP_META.VERSION` to `1.6.0` and `LAST_UPDATED` to `2026-05-12` in [packages/constants/src/config.ts](packages/constants/src/config.ts)
- [x] Synced [package.json](package.json), [apps/web/package.json](apps/web/package.json), and [functions/package.json](functions/package.json) to `1.6.0`
- [x] Created root [CHANGELOG.md](CHANGELOG.md) in Keep-a-Changelog format, generated by `pnpm changelog:sync` from the typed source (no hand-editing — re-run the script to refresh)
- [x] Refactored [ChangelogSection.tsx](apps/web/src/components/common/UserGuide/ChangelogSection.tsx) to import from `@vapour/constants` instead of hardcoding entries
- [x] Added a `removed` change type so deprecations show up clearly in both the in-app guide and the markdown changelog
- [x] Typecheck (`@vapour/constants`, `@vapour/web`) and `pnpm lint` pass clean
- [ ] Tag git releases with semver (`git tag -a v1.6.0 -m "..."`) — manual step, do when ready to cut the release
- [x] Added `pnpm check:last-updated` script at [scripts/check-last-updated.ts](scripts/check-last-updated.ts) — fails (exit 1) if `APP_META.LAST_UPDATED` is more than 7 days behind HEAD. Wire into CI or husky pre-push when ready; currently invokable manually
- [x] Keep dated feature guides (`*-GUIDE-YYYY-MM-DD.md`) as-is — they are point-in-time snapshots, distinct from the rolling changelog

---

## 4. Bank reconciliation — DELETED (2026-05-12)

**Status:** Fully removed. All four Firestore collections (`bankStatements`, `bankTransactions`, `reconciliationMatches`, `reconciliationReports`) were verified empty in production, and the permission flag was already marked `_DEPRECATED_` client-side, so full deletion was preferred over hiding.

- [x] Verified production Firestore collections are empty (0 docs in all four)
- [x] Removed dashboard tile and `ReconciliationIcon` import from `apps/web/src/app/accounting/page.tsx`
- [x] Deleted `apps/web/src/app/accounting/reconciliation/` (page + 10 components)
- [x] Deleted `apps/web/src/lib/accounting/bankReconciliation/` (9 service files + tests)
- [x] Deleted `apps/web/src/lib/accounting/autoMatching/` (8 files including scoring/batch/utils)
- [x] Deleted `apps/web/src/lib/accounting/bankStatementParser.ts` and its test
- [x] Deleted `packages/types/src/bankReconciliation.ts` and its export from the types barrel
- [x] Removed `bankReconciliationKeys` from `apps/web/src/lib/queryKeys/accounting.ts` and `index.ts`
- [x] Removed `createMockBankTransaction` from test-utils factories and exports
- [x] Removed `bankStatementParser` and `bankReconciliation` re-exports from `apps/web/src/lib/accounting/index.ts`
- [x] Removed `RECONCILE_ACCOUNTS` from `functions/src/userManagement.ts` and the deprecated marker from `packages/constants/src/permissions.ts`
- [x] Removed Firestore rules for the four collections plus the standalone `bankReconciliation` rule
- [x] Removed three composite indexes from `firestore.indexes.json`
- [x] Updated `apps/web/src/lib/accounting/README.md` and `apps/web/src/test-utils/README.md`
- [x] Typecheck (`@vapour/types`, `@vapour/constants`, `@vapour/web`, `functions`) and `pnpm lint` all pass

**Deploy notes:** `firebase deploy --only firestore:rules,firestore:indexes` is needed to drop the obsolete server-side rules and indexes. The empty Firestore collections will not be deleted by the deploy — they will simply become inaccessible. They can be left in place or removed via the Firebase console if a clean slate is preferred.

---

## Suggested execution order

1. ~~**Versioning consistency**~~ — done
2. ~~**Sync changelog + thermal expansion guide**~~ — done
3. **Thermal MCP server Phase 1** — only remaining item; defer until a procurement / estimation use case needs it (the agent infrastructure from v1.6.0 Phase 0 is already in place)
4. **Tag `v1.6.0` in git** — manual, do when cutting the release
