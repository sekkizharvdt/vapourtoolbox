{
  "name": "functions",
  "version": "1.6.0",
  "description": "Cloud Functions for VDT-Unified",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node build-postprocess.js",
    "build:watch": "tsc -p tsconfig.build.json --watch",
    "test": "jest",
    "test:integration": "npm run build && cd .. && firebase emulators:exec --only firestore,functions --config firebase.test.json --project test-project \"cd functions && npx jest -c jest.integration.config.js\"",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@google-cloud/documentai": "^10.0.0",
    "@sparticuz/chromium": "^149.0.0",
    "archiver": "^8.0.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.3.2",
    "handlebars": "^4.7.9",
    "nodemailer": "^9.0.5",
    "puppeteer-core": "^25.5.0"
  },
  "devDependencies": {
    "@types/archiver": "^8.0.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^22.10.1",
    "@types/nodemailer": "^8.0.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.12",
    "typescript": "^5.2.0"
  },
  "private": true,
  "comment_overrides": "Security overrides for the functions npm tree (used by Firebase deploy's `npm ci` — the root pnpm overrides do NOT apply here). Same-major fixes chosen to avoid runtime breakage in the deployed backend. minimatch is intentionally NOT forced: its vulnerable 5.x/9.x instances sit beside a safe 3.1.2 (glob@3), so a global force would break that consumer; the advisory is a ReDoS in path-matching on trusted inputs, low risk for a backend.",
  "overrides": {
    "fast-xml-parser": "^5.7.0",
    "@grpc/grpc-js": "^1.14.4",
    "form-data": "^2.5.6",
    "protobufjs": "^7.6.4",
    "path-to-regexp": "~0.1.13",
    "node-forge": "^1.4.0",
    "basic-ftp": "^5.3.1",
    "ws": "^8.20.1",
    "lodash": ">=4.18.0"
  }
}
